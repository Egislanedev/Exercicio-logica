// Faca um algoritmo que leia os valores A, B, C e imprima na tela se a soma de A + B e menor
// que C.

let A = 3; //Criei as variaveis A B e C, com os respectivos valores: 3, 2 e 10;
let B = 2;   
let C = 10;


soma = A + B;

if((A + B) < C) {    //se essa condição for verdadeira, ou seja, "se A + B < C" (true)
 
    document.write("A soma de ", A,'+',B, "é menor que", C); // Irá aparecer na tela o resultado da soma com a seguinte mensagem: "A soma de 3+2 é menor que 10"

}
